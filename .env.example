# REQUIRED - Generate with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
APP_SECRET_KEY=CHANGE_ME_GENERATE_RANDOM_STRING

# REQUIRED - Generate with: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
DATA_ENCRYPTION_KEY=CHANGE_ME_GENERATE_FERNET_KEY

# REQUIRED - Hugging Face write token (for database sync)
HF_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxxxxx

# REQUIRED - HF Dataset repo (private)
HF_DATASET_REPO=username/edusmart-xiaozhi-db

# OPTIONAL
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
ALLOWED_HOSTS=*
